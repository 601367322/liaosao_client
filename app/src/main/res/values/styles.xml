<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
        <item name="colorPrimaryDark">@color/holo_blue_dark</item>
        <item name="colorPrimary">@color/holo_blue_light</item>
        <item name="colorAccent">@color/holo_blue_light</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="actionMenuTextAppearance">@style/MyABmenuTextAppearance</item>
    </style>

    <style name="AppBaseThemeNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/holo_blue_dark</item>
        <item name="colorPrimary">@color/holo_blue_light</item>
        <item name="colorAccent">@color/holo_blue_light</item>
        <item name="actionMenuTextAppearance">@style/MyABmenuTextAppearance</item>
    </style>

    <style name="AppBaseThemeNoActionBar5" parent="AppBaseThemeNoActionBar">
    </style>

    <style name="MyActionBarStyle" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="displayOptions">homeAsUp|showTitle</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- menu -->
    <style name="MyABmenuTextAppearance" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="Dialog_loading_noDim" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <!-- 无边框 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 没有标题 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 背景是否半透明 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 背景是否模糊 -->
    </style>
</resources>
